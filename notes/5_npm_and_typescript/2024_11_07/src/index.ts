// index.ts
// TypeScript Notes and Examples

function sum(a: number, b: number, c: number): number{
    return (a + b + c);
}

let y: number = 10;
let x: number = sum(5.8, 2.3, 10.5);
console.log(`sum = ${x}`);